[zram0]

# Halve size to reduce zram processing overhead
zram-size = ram / 3

zram-resident-limit = 0
host-memory-limit = none
compression-algorithm = zstd
# writeback-device = none
swap-priority = 32766
options = async discard exec noatime nodiratime nosuid
